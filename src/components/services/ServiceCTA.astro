---
import { Icon } from "astro-icon/components";
import FormLink from "@/components/contact/FormLink.astro";
import WhatsappIcon from "@/components/social-icon/WhatsappIcon.astro";
import { Button } from "@/components/ui/button";
import { whatsappUrl } from "@/constants";
import type { ServiceData } from "@/data/servicios";

interface Props {
	service: Pick<ServiceData, "ctaLabel">;
	footerText: string;
	secondaryHref?: string;
	secondaryLabel?: string;
	/** Mensaje precargado de WhatsApp para el CTA. */
	waMessage?: string;
	/**
	 * Destino del enlace al formulario. Por defecto navega a `/contacto/`;
	 * en páginas con el formulario embebido se pasa `#formulario`.
	 */
	formHref?: string;
}

const {
	service,
	footerText,
	secondaryHref,
	secondaryLabel = "Ver servicio completo",
	waMessage,
	formHref,
} = Astro.props;
---

<div class="mt-7 flex flex-wrap items-center gap-3">
	<Button
		as="a"
		href={whatsappUrl(waMessage)}
		target="_blank"
		rel="noopener noreferrer"
		variant="whatsapp"
		size="lg"
		class="gap-2"
		data-umami-event="whatsapp_click"
		data-umami-event-position="service-card"
	>
		<WhatsappIcon size={18} />{service.ctaLabel}
	</Button>
	{secondaryHref && (
		<Button as="a" href={secondaryHref} variant="outline" size="lg" class="gap-2">
			<Icon name="hugeicons:arrow-right-01" class="size-4" />{secondaryLabel}
		</Button>
	)}
	<FormLink position="service-cta-form" href={formHref} />
</div>
<p class="mt-4 text-[11px] text-muted-foreground/70">
	{footerText}
</p>
